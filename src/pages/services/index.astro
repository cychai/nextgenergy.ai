---
import Base from '@/layouts/Base.astro';
import PageHero from '@/components/PageHero.astro';
import Card from '@/components/Card.astro';
import { services } from '@/data/sections';
---
<Base title="Services" description="NextGenergy services: thermal design and selection, integration and factory pre-assembly, commissioning and verification, operations and long-term support.">
  <PageHero eyebrow="Services" title="What we can do is worth more than what we sell." lede="Four stages, one team, one set of records. Each can be engaged on its own. Commissioning in particular is often the first thing a new customer buys from us, for a system somebody else built." />
  <section class="section">
    <div class="wrap">
      <div class="grid c2">
        {services.map((s, i) => <Card href={`/services/${s.slug}`} num={String(i + 1).padStart(2, '0')} title={s.title}>{s.summary}</Card>)}
      </div>
    </div>
  </section>
</Base>
